name: BTM build

on:
  push:
    branches: [ multi ]
  pull_request:
    branches: [ multi ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'
      - uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build with Gradle
        run: ./gradlew build
      - name: Stop gradle daemon
        run: ./gradlew --stop
      - name: Copy jars
        run: |
          cp ./**/build/libs/BetterThanMending*.jar .
          cp ./build/forgix/BetterThanMending*.jar .
      - uses: actions/upload-artifact@v7
        with:
          name: BTM Jars
          path: ./*.jar